#include <bits/stdc++.h>
#include <bits/debug.h>

using namespace std;

int main() {
	int t;
	cin >> t;

	while (t--) {
		int n, m;
		cin >> n >> m;

		while (m--) {
			while (n--) {
				int x;
				cin >> x;
			}
		}

		cout << "hello\n";
	}
}
