#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        cout << 1 1 << endl;

        while (true) {
            string move;
            cin >> move;
            if (move == "Done") {
                break;
            }

            if ()
        }
    }
}